name: Trigger Jenkins Job

on:
  push:
    branches: [main]

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST "https://${{ secrets.JENKINS_URL }}/job/${{ secrets.JENKINS_JOB }}/build" \
            --user "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_TOKEN }}" \
            --insecure
